# Daily-Driver Fixture

Copy this directory to a temporary workspace before running packaged QA. The expected agent edit changes the heading in `index.html` from `KEELHOUSE DAILY DRIVER READY` to `KEELHOUSE DAILY DRIVER COMPLETE`. Serve the temporary copy locally; never ask an agent to modify the checked-in fixture.
